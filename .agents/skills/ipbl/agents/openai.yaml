interface:
  display_name: "IPBL"
  short_description: "Repo-specific patterns and workflows for IPBL"
  default_prompt: "Use the IPBL repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true