#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO="${MELLONELAY_REPO:-$(cd "$SCRIPT_DIR/.." && pwd)}"
exec python3 "$REPO/scripts/agenteam_orchestrate.py" "$@"
